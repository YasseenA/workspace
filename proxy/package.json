{
  "name": "workspace-proxy",
  "version": "1.0.0",
  "description": "CORS proxy for Canvas LMS + Anthropic API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
